import React, { FC } from "react";
import { render } from "react-dom";

const App: FC = () => {
  return <div>Service Worker Webpack Plugin: React Example</div>;
};

render(<App />, document.getElementById("app"));
